import { WeaveDirective } from './weave.directive';

describe('WeaveDirective', () => {
  it('should create an instance', () => {
    const directive = new WeaveDirective();
    expect(directive).toBeTruthy();
  });
});
