import { KeycodesDirective } from './keycodes.directive';

describe('KeycodesDirective', () => {
  it('should create an instance', () => {
    const directive = new KeycodesDirective();
    expect(directive).toBeTruthy();
  });
});
