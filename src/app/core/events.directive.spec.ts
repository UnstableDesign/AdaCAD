import { EventsDirective } from './events.directive';

describe('EventsDirective', () => {
  it('should create an instance', () => {
    const directive = new EventsDirective();
    expect(directive).toBeTruthy();
  });
});
